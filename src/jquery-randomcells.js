// Generated by CoffeeScript 1.8.0
(function() {
  (function($) {
    return $.fn.randomCells = function(options) {
      var $wrapper, elements, init, readSrc, selector, settings, spread, src;
      settings = $.extend({
        'max': 3,
        'delay': 500,
        'selector': 'li',
        'src': '#rc-data'
      }, options);
      $wrapper = this;
      selector = settings.selector;
      src = settings.src;
      elements = [];
      readSrc = function() {
        return $(src).find(selector).each(function() {
          return elements.push($(this));
        });
      };
      spread = function() {
        return console.info("spread!");
      };
      init = function() {
        readSrc();
        return spread();
      };
      return init();
    };
  })(jQuery);

}).call(this);
